[project]
name = "openshell-ogx-example"
version = "0.1.0"
description = "OpenShell + OGX (Llama Stack) sandbox integration examples"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.80.0",
    "httpx>=0.27.0",
]
